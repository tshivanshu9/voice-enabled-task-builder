export const PRIORITY_ENUM = {
  LOW: 'low',
  MEDIUM: 'medium',
  HIGH: 'high',
}

export const PRIORITY_VALUES = Object.values(PRIORITY_ENUM);

export const TASK_STATUS_ENUM = {
  TODO: 'to-do',
  IN_PROGRESS: 'in-progress',
  DONE: 'done',
}

export const TASK_STATUS_VALUES = Object.values(TASK_STATUS_ENUM);

export const ACTIVE_INACTIVE_ENUM = {
  ACTIVE: 'Active',
  INACTIVE: 'Inactive',
}

export const ACTIVE_INACTIVE_VALUES = Object.values(ACTIVE_INACTIVE_ENUM);